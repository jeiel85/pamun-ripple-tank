# Backlog — 알려진 한계와 미룬 개선

항목마다 **무엇을 · 왜 · 영향 범위**를 한 줄로 적습니다. 수치는 2차 리뷰와 그 수정 라운드에서 잰 값입니다(헤드리스 Chrome, 시뮬 시간 기준 결정적 재현 포함).
명세와 다르게 구현한 곳은 [docs/DEVIATIONS.md](docs/DEVIATIONS.md)에 따로 정리했습니다.

## 미충족 수용 기준

- [ ] **수용 기준 2 · 첫 프레임 톤**: 밝은 픽셀(B ≥ 0.70) 비율이 목표 8~18%의 하한에 못 미칩니다. 첫 프레임은 데스크톱 0.066·폰 0.072이고, 정상 상태는 0.074~0.083입니다(`diag().tone.bright`). 전역 대비 제한 min(1, 0.14/m)이 걸리는 동안에는 κ를 키워도 밝은 비율이 분포 모양으로만 정해지므로, 대비 목표식이나 LUT 쪽을 손봐야 합니다. 영향 범위는 첫인상의 밝기(렌더링)입니다.
- [ ] **수용 기준 7 · 메아리 북 4번째 메아리**: 중앙 울림돌 타격이 0.006 / 0.997 / 1.999 / 2.996 / 3.859박입니다. 4번째가 −0.141박(약 −100 ms, −3.5%)으로 ±2%를 벗어납니다. 반사를 거듭하면서 파형이 회전해 가장 밝은 로브가 바뀌기 때문입니다. 영향 범위는 프리셋 '메아리 북'의 5번째 타격 이후입니다. (R1-4는 부분 수정: 이중 타격은 해소됐습니다.)
- [ ] **수용 기준 17 · 캘리퍼 직선 경로 ±3%**: 첫 장면에서 청황은 −7~−8%이고, 포물선 초점 근처 임은 6/12/20/30 s에 −20/−2/+13/−2%로 흔들립니다. 반사파가 직선 도달과 섞여 무리 평균이 흔들리기 때문입니다. 영향 범위는 첫 장면 캘리퍼 라벨입니다.

## 미룬 개선

- [ ] **판독 확정 지연 정책(READ_LAG)**: `READ_LAG = 0.06`은 로브 분석 경로만 덮습니다. 첫 장면·공명실에서는 확정이 최대 0.15~0.21 s까지 늦어져 일부 타격(첫 장면 약 27%, 공명실 대부분)이 하한에 잘려 늦게 울립니다(`diag().lateStrikes`). READ_LAG를 올리면 탭 소리가 더 늦어지므로, 판독 확정을 앞당기거나(후보 로브가 끝나면 진폭 패턴으로 바로 확정) 타임아웃을 줄이는 방향을 검토합니다. 바꾸면 유리 두께별 링당 1회와 메아리 북 검증을 다시 돌립니다. 영향 범위는 오디오 타이밍(수용 기준 5)입니다.
- [ ] **탭 소리 지연 +60 ms**: 단일 시계라서 READ_LAG가 탭 '똑'에도 더해져 입력부터 예약까지 약 75~89 ms가 걸립니다(감쇠 모드는 약 +150 ms). 위 정책 결정과 함께 줄입니다. 영향 범위는 치는 느낌(1장 약속 2)입니다.
- [ ] **새 진동자 램프 약 5 s**: 처음 1.5 s는 진폭이 8% 미만이라 붐비는 장면에서는 놓아도 잘 보이지 않습니다. 램프를 빨리 하면 느린 포락선 es가 따라오지 못해 주변 울림돌이 연타합니다(τ 1.1 s·시작 10%로 바꾸면 2 s 안에 가짜 타격 29~46회). 새 진동자 근처 울림돌의 es를 예상 연속 진폭으로 미리 채우는 방식을 검토합니다. 되돌리기로 복원한 진동자도 같은 이유로 3%에서 다시 시작합니다. 영향 범위는 진동자 배치의 즉시성입니다.
- [ ] **병합 창 예외의 한계**: 직선 도달 예측과 맞는 트리거는 병합하지 않으므로, 빠른 분할의 점적기가 많아 예측 창(−25~+50 ms)이 촘촘히 겹치면 유리 뒤 분산 꼬리가 다시 두 번 칠 수 있습니다. 벽 반사로 오는 두 번째 링은 여전히 병합 대상입니다. 영향 범위는 복잡한 배치의 타격 수입니다.
- [ ] **30 fps 기기의 q0↔q1 탐색**: 내림 조건은 vsync에 묶인 30 fps를 느리다고 보지 않게 고쳤습니다(모든 단계가 33.3 ms일 때 10분 동안 전환 12회 → 4회, 이후 q0에 안착). 하지만 q0만 50 ms이고 q1이 33.3 ms인 기기에서는 올림 시도와 되내림이 지수 백오프 간격으로 이어집니다(10분 13회). 올림 판정에 '그 단계에서 실제로 쟀던 간격'을 기억하는 방식을 검토합니다. 영향 범위는 저사양 폰의 렌더 해상도 변화입니다.
- [ ] **뷰 회전 식 사본 5곳**: `toCss`·`toTank`·`screenDirToTank`·`screenToTankMat3`·`renderFallback`의 setTransform이 같은 회전 규칙을 따로 들고 있습니다(주석으로 연결만 해 둠). 행렬 하나에서 파생하도록 합칩니다. 영향 범위는 입력·오버레이·투영의 일치입니다.
- [ ] **이름 없는 명세 수치**: g_auto(0.97·0.15·0.01), DC 제거·NaN 점검 주기(60·30프레임), 판독 포락선 τ(0.40·0.25 s), 대비 목표 0.14, 보이스 상한(28·40·12) 같은 수치가 리터럴로 남아 있습니다. 모듈 머리에 명세 절 번호를 붙인 상수로 모읍니다. 영향 범위는 유지보수입니다.
- [ ] **`#tools-more` 배치(R2-6, 1차 리뷰에서 이월)**: '장치 ▸' 버튼이 도구 `role="radiogroup"` 안에 radio가 아닌 button으로 들어 있습니다. 보조 기술이 그룹 구성을 잘못 읽을 수 있으므로 그룹 밖으로 옮기되, 시각 배치는 그대로 둡니다. 영향 범위는 스크린 리더 접근성입니다.
